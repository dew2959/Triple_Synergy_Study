# 04 자료구조
## 04 -1 배열과 리스트
> 배열  
- 메모리 연속 공간에 값이 채워져 있는 형태의 자료 구조
- 값은 인덱스를 통해 참조 가능하며, 선언한 자료형의 값만 저장 가능하다.
- 새로운 값을 삽입하거나 특정 인덱스에 있는 값을 삭제하기 어렵다
- 배열의 크기는 선언 시에 지정 가능하며, 한 번 선언하면 크기 조정 불가능

> 리스트
- 노드(값과 포인터를 묶은 것)를 포인터로 연결한 자료구조
- 인덱스가 없기에 값에 접근하려면 Head 포인터부터 순서대로 접근해야 한다.
- 포인터로 연결되어 있기에 데이터 삽입 및 삭제 연산 속도가 빠르다.
- 선언 시에 크기 지정 불필요. 크기가 정해져 있지 않다.
- 포인터를 저장할 공간이 필요해 배열보다 구조가 복잡

#### *** 파이썬에서의 List는 리스트와 배열의 특징을 모두 가지도록 구현되어 있다.


> 문제 1. 숫자의 합 구하기
> -
    N개의 숫자가 공백 없이 쓰여 있다. 
    이 숫자를 모두 합해 출력하는 프로그램 작성하시오.

    [예제]
    5  # 숫자의 개수
    54321 # 공백 없이 작성된 5개의 숫자

- int는 공백 기준으로 list를 만들기 때문에 문자열을 쪼개는 방식을 사용해야 함

```python
number_count = int(input())
numbers = list(input())
sum = 0

for number in numbers:
    sum += int(number)
    
print(sum)
```